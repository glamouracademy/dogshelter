<%= form_for(@dog) do |f| %>
  <% if @dog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dog.errors.count, "error") %> prohibited this dog from being saved:</h2>

      <ul>
      <% @dog.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :name %>:
  <%= f.text_field(:name) %><br>
  <%= f.label :breed %>:
  <%= f.text_field(:breed) %><br>
  <%= f.label :weight %>:
  <%= f.text_field(:weight) %><br>
  <%= f.label :age %>:
  <%= f.text_field(:age) %><br>
  <%= f.select(:shelter_id, @shelters) %> <br>
  <%= f.select(:adopter_id, @adopters) %>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
